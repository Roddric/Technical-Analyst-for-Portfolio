# Core pipeline (pandasta_* / portfolio_backtest / ta_advisor / stats)
numpy>=2.5
pandas>=3.0
scipy>=1.18
pandas-ta-openbb>=0.4.24   # imports as `pandas_ta` (OpenBB-maintained fork)
yfinance>=1.5
pytest>=9.1                # test suite (tests/)
pandas-ta-classic>=0.6.52  # tests/test_registry.py exercises the 'ta' accessor-conflict guard against it
tabulate>=0.10             # pandas .to_markdown() in report writers
